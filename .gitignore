# Secretos — nunca commitear
.env
.env.*
!.env.example

# Enlace local al proyecto de Vercel
.vercel

# Dependencias y builds
node_modules/
.next/
dist/
out/
target/

# Sistema
.DS_Store
Thumbs.db

# Stellar CLI: identidades locales (contienen semillas secretas)
.stellar/

# Web build
*.tsbuildinfo

# Material de postulaciones (becas, hackathones): privado, nunca se publica.
# Borrar la carpeta una vez enviada la postulación.
postulacion/
